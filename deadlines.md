# Canvas Deadlines — living memory

Last updated: (never yet — this is the empty starter template)

Status meanings: `upcoming` = not due yet · `done` = submitted per Canvas · `past` = due date passed.
The **Event ID** column holds the Google Calendar event ID once posted — a row with an ID must never get a second event.

| Course | Assignment | Due date | Status | Event ID | Notes |
|--------|-----------|----------|--------|----------|-------|

<!-- Runs append rows above this line. Keep the table sorted by due date. -->
